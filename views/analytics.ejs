<%- include("header") -%>

<section id="analytics">
    <div class="container-fluid stat-box">
        <h2>Analytics</h2>
        <div class="table-div">
        <table>

            <thead>
                <th>Full Url</th>
                <th>Short Url</th>
                <th>Clicks</th>
            </thead>
            <tbody>
               <% urls.forEach(url => { %>
              <tr>
                <td><a href="<%= url.longUrl %>" target="_blank"><%= url.longUrl %></a></td>
                <script>
                  function myfunction() {
                    var copyText = document.getElementById("text");
                 
                    copyText.select();
                    copyText.setSelectionRange(0, 9999);
                 
                    navigator.clipboard.writeText(copyText.a)
                 
                    alert("Copied the Text : " + copyText.a);
                  }
                 </script>
                <div class="copy-text"><td><a id="text" href="<%= url.shortUrl %>" target="_blank"><%= url.shortUrl %></a>
                <button class="copy" onclick="myfunction()">Copy
                  <i class="fa fa-clone"></i>
                </button>
                </td>
              </div> 
                <td><%= url.clicks %></td>
              </tr>
            <% }) %>
            </tbody>
    
    
        </table>
    </div>
    </div>
</section>
<%- include("footer") -%>
